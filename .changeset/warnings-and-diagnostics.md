---
"@cc-ts/builder": patch
---

Emit warnings instead of throwing for non-fatal issues such as unresolved JavaScript packages and Node builtins. New diagnostics cover invalid `explain`/`externals`/`analyzeFormat` values and minify+sourceMap incompatibility.
